# HARNESS_CHANGELOG.md — ハーネス改訂台帳(toukei-atlas)

原則: **エージェントがミスをするたびに、そのミスが二度と起きないようハーネスを改良する。**
起票条件: 同一失敗コード累計 2 回(LL-10)、または severity S1(LL-12)。

---

## HC-001

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-19 |
| トリガー | `VERIF-FALSE` × 2(toukei-atlas loop_002: ①gpkg 幾何列名を geometry と仮定(実際は geom)②T-009 の照合基準に北方領土を含む都道府県公表面積を採用し北海道が −6% 乖離) |
| 診断 | オラクルテストの前提(出力の列名・照合する公表値の定義域)を、実データを検分せずに記述していた。特に「公表値」はそれ自体に集計範囲の定義(北方領土の包含など)があり、採録範囲と一致するとは限らない |
| 改訂 | TEST_SPEC.md 実行規約に追加: 「オラクルテストを書く前に、(a) 照合対象の実出力のスキーマ(列名・型)を読み、(b) 公表値を基準にする場合はその**集計範囲の定義**(何を含み何を含まないか)を確認し、テスト内コメントに残す。範囲が採録と異なる場合は基準を採録単位の合算に置き換える」 |
| 種別 | test_spec_template |
| SCAFFOLD_VERSION | 1.8.0(レジストリ改訂は還流判断待ち — フリート展開するなら registry/templates/TEST_SPEC.md.tmpl へ) |
| 効果検証 | 以後 5 ループで VERIF-FALSE の再発 0 件なら Closed |
| propagation | toukei-atlas ✅(本文書 + TEST_SPEC.md)/ レジストリ還流 ⬜(人間の承認待ち) |
| 状態 | Open |
