# TEST_SPEC.md — toukei-atlas

<!-- scaffold template v1.8.0 から展開(2026-08-19)。本プロジェクトは R のため実行規約を testthat に読み替える -->

## 実行規約

- `Rscript build/verify.R`(内部で `testthat::test_dir("tests/testthat")`)を stage 3–5 の判定に使用
- テストは 3 層:`unit`(純関数)/ `integration`(パイプライン結合)/ `validation`(公表値オラクル)
- フィクスチャ更新(SVG ハッシュの期待値更新を含む)は専用コミット(`test: update fixtures`)で行い、
  **なぜ絵が変わったのか**をループログに記す。理由を説明できないハッシュ更新は禁止
- 公表値を期待値に使うテストは、**出典 URL と取得日をテスト内コメントに残す**。
  前提を検算しない期待値は正しい実装を落とす(VERIF-FALSE / HC-004)

## オラクルの構造

本プロジェクトのオラクルは「実装と独立した正解の源」を 3 層で積む:

1. **外部公表値**(最強)— e-Stat・総務省統計局・国土地理院の公表数値との照合。G-01/G-03/G-06
2. **構造不変量** — コード集合の一致・幾何の妥当性・スケールの単調性。G-02/G-05
3. **決定論回帰** — 同一入力 → 同一バイト出力。G-04

## ケース一覧

| ID | 対応要求 | 層 | ケース | 期待 |
|---|---|---|---|---|
| T-001 | G-01/F-01 | validation | 市区町村の総人口を都道府県別に合算 | e-Stat 公表の都道府県人口と全 47 件一致(誤差 0) |
| T-002 | G-01 | validation | 都道府県人口を合算 | 2020 国勢調査 全国人口 126,146,099 人に一致 |
| T-003 | G-01/F-01 | validation | 高齢化率を自前計算(65 歳以上 ÷ 総人口) | e-Stat 公表の高齢化率と ±0.1pt 以内 |
| T-004 | G-02 | integration | 境界データと統計 CSV の市区町村コード集合 | 差集合が空(結合漏れ 0・孤児コード 0) |
| T-005 | G-02 | integration | 結合後の行数 | 境界データの地物数と一致(重複結合 0) |
| T-006 | G-02 | integration | 政令市の粒度 | 境界・統計とも区単位(例: 14100 横浜市の行が存在しない、14101 鶴見区が存在する) |
| T-007 | G-03 | integration | `sf::st_is_valid()` | 全ポリゴン TRUE |
| T-008 | G-03 | integration | CRS | 結合済み sf の CRS が JGD2011(EPSG:6668) |
| T-009 | G-03 | validation | `st_area` の都道府県合算 | 国土地理院「全国都道府県市区町村別面積調」と誤差 1% 以内 |
| T-010 | G-04/N-01 | integration | 同一入力で 2 回レンダリング | SVG のバイト列が一致 |
| T-011 | G-04 | integration | フィクスチャ入力(3 市区町村のミニ地図)のレンダリング | 保存済み期待 SVG とハッシュ一致 |
| T-012 | G-05 | unit | 凡例端点 | 色スケールの limits がデータ min/max(固定端点テーマは定義値)と一致 |
| T-013 | G-05 | unit | 色割当の単調性 | 値の昇順に対し viridis の明度順序が単調 |
| T-014 | G-05/F-03 | unit | 発散スケールの中点 | 増減率 0 に中立色が割り当たる |
| T-015 | G-06 | validation | 市区町村人口の最大 | 横浜市…ではなく**区単位のため世田谷区**が特別区最大・市単位集計を別途持つ場合は横浜市。粒度を明示して assert |
| T-016 | F-08 | integration | 各テーマページの HTML | 地図・棒グラフ・ヒストグラム・注記の 4 ブロックの DOM が存在 |
| T-017 | F-08 | unit | ランキング抽出 | 上位 10 + 下位 10 件、値の降順/昇順が正しい、同値時は市区町村コード順で安定 |
| T-018 | F-10 | unit | フッタ定義 | リンクが規定の順序・全 https・ラベル非空。未公開アーティファクトの行が出力に混ざらない |
| T-019 | F-12 | integration | 注記ブロック | 調査名・年次・分母分子・取得日が全ページに存在 |
| T-020 | F-13 | integration | data/pins.csv | 全取得物の SHA256 が実ファイルと一致 |
| T-021 | N-04 | integration | ページサイズ | out/ の各 HTML+同梱 SVG 合計が 2 MB 以下 |
| T-022 | F-02 | unit | 人口密度の計算 | 手計算フィクスチャ(人口 1000・面積 4 km² → 250 人/km²)と一致 |
| T-023 | F-03 | unit | 増減率の計算 | 手計算フィクスチャ((900−1000)/1000 → −10.0%)と一致・2015 人口 0 の市区町村(合併等)の扱いが定義どおり |

註 T-015: 粒度の疑義(市か区か)はオラクル設計上の既知の罠。loop_001 の文脈読込で
使用データの粒度を確定させてから期待値を固定する。
